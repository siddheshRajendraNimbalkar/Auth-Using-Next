export const publicRouth = [
    "/"
];

export const authRouth = [
    "/auth/login",
    "/auth/register"
];

export const apiAuthPrefix = "/api/auth";

export const defaultAuthRedirect = "/setting";